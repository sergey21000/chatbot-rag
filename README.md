

# Telegram бот для рассылки новостей

Telegram бот который парсит рассылку из электронной почты от Хабр и Proglib AI и пересылает ее в указанные чаты  
Так же бот может рассылать напоминания по расписанию

В Google Colab <a href="https://colab.research.google.com/github/sergey21000/telegram-stt-tts-bot/blob/main/Chatbot_Telegram_TTS_STT_llamacpp_Docker.ipynb"><img src="https://img.shields.io/static/v1?message=Open%20in%20Colab&logo=googlecolab&labelColor=5c5c5c&color=0f80c1&label=%20" alt="Open in Colab"></a> ноутбуке находится код бота с комментариями, процесс получения и парсинга писем из почты, деплой бота на Android и подключение терминала для удобной работы с телефоном

---
## 📋 Содержание

- 🚀 [Функционал](#Функционал)
- 🏗 [Стек](#Стек)
- ⚙ [Подготовка к запуску](#Подготовка-к-запуску)
- 🐍 [Запуск на Linux/Windows](#Запуск-на-LinuxWindows)
- 📱 [Запуск на Android](#Запуск-на-Android)
  - [Установка Termux и python на Android](#Установка-Termux-и-python-на-Android)
  - [Установка wkhtmltopdf на Android для работы с PDF](#Установка-wkhtmltopdf-на-Android-для-работы-с-PDF)
  - [Запуск бота на Android](#Запуск-бота-на-Android)
- 🔧 [Настройка рассылки](#Настройка-рассылки)
- 🛠 [Дополнительные настройки рассылки](#Дополнительные-настройки-рассылки)


---
## 🚀 Функционал

- Возможность установки расписания для всех типов рассылок 
- Рассылка новостей из почтовой рассылки от Хабр в текстовом формате
- Рассылка статьи из почтовой рассылки от Proglib AI в формате PDF и ссылкой на веб-версию статьи
- Возможность рассылки самописных напоминаний
- Возможность создания своих конфигов рассылки из электронной почты

Как работает бот:
1) Письма с рассылками новостей приходят на элетронную почту mail.ru
2) При старте бота запускается планировнщик, который активирует парсинг почты по расписанию
3) Парсер получает содержимое писем и формирует из них текст для рассылки, для рассылки от Хабр это просто текст со списком новостей, для рассылки от Proglib AI это web-ссылка на статью и PDF документ с этой статьей
4) Рассылка производится по указанным в конфигах чатам
5) Можно настроить рассылку своих кастомных сообщений, для этого нужно добавить свои конфиги


<details>
<summary>Скриншот рассылки от Хабр и Progliob AI</summary>

![Рассылка](./screenshots/рассылки.png)
</details>


---
## 🏗 Стек

- [python](https://www.python.org/) >= 3.10
- [aiogram](https://github.com/aiogram/aiogram) для написания Telegram бота
- [aioimaplib](https://github.com/bamthomas/aioimaplib) для асинхронной работы с почтовым ящиком
- [beautifulsoup4](https://pypi.org/project/beautifulsoup4/) для парсинга содержимого писем
- [python-pdfkit](https://github.com/JazzCore/python-pdfkit) и используемая под капотом [wkhtmltopdf](https://github.com/wkhtmltopdf/wkhtmltopdf) для преобразования веб страницы с новостями от Proglib AI в PDF документ

Работоспособность приложения проверялась на Ubuntu 22.04 (python 3.10.12), Windows 10 (python 3.12.2) и Android 11 (Termux, python 3.12.7) 


---
## ⚙ Подготовка к запуску

**1) Клонирование репозитория**  
```
git clone https://github.com/sergey21000/telegram-email-news-bot.git
cd telegram-email-news-bot
```

**2) Регистрация бота, получение пароля почты**  

Создать файл `.env`
```
BOT_TOKEN=your_bot_token
MAIL_ADRESS=your_mail_adress@mail.ru
MAIL_PASSWORD=your_mail_password
```

Заполнить файл `.env` следующим содержимым

1. В переменную `BOT_TOKEN` записать токен бота, полученнй у https://t.me/BotFather  
Для получения токена отправить боту https://t.me/BotFather команду /newbot, ввести любое имя бота, затем ввести имя пользователя бота (должно заканчиваться на `bot`), затем скопировать токен бота, записать в переменную `BOT_TOKEN`  
Нажать на никнейм бота и нажать кнопку запустить, начав таким образом диалог с ботом

2. В переменную `MAIL_ADRESS` записать свой адрес почты https://e.mail.ru/

3. Создать пароль для внешних приложений почты и записать в переменную `MAIL_PASSWORD`  
Для создания пароля перейти на https://e.mail.ru/inbox, нажать на кружок аккаунта в правом верхнем углу - `Пароль и безопасность` - `Пароли для внешних приложений` - `Создать` - ввести любое название - выбрать `Только чтение и удаление писем в Почте`

**3) Подписка на рассылку новостей**

Можно выбрать обе или одну из двух готовых рассылок ниже или вообще ни одной, если бот преназначен для рассылки своих напомининий

**Рассылка от Хабр**

Чтобы подписаться на расслыку от Хабр, нужно зайти на https://habr.com/, зарегистрироваться и войти в аккаунт, перейти в настроки уведомлений https://habr.com/ru/settings/notifications/ и выбрать частоту рассылки дайджеста внизу (например Каждый день)  
Подписаться на интересующие темы для рассылки можно на этой странице
https://habr.com/ru/feed/settings/

Конфиги уже настроены чтобы парсить рассылку от Хабр, которая автоматически приходит в папку писем Новости

**Рассылка от Proglib AI**

Подписаться на рассылку от Proglib AI можно здесь
https://proglib.academy/ai-newsletter

Затем нужно настроить чтобы письма от Proglib AI приходили в определенную папку почты с названием proglib

Для этого нужно перейти на https://e.mail.ru/inbox, нажать `Настройки` в левом нижнем углу - `Все настройки` - `Фильтры` - `Добавить фильтры  `
В настройках фильтра записать в `Поле от содержит` строку `hello@proglib.academy`  
В `Тогда` `переместить в папку` нажать на `Входящие` и выбрать `Новая папка`, и в поле `Папка на верхнем уровне` написать название `proglib`


<details>
<summary>Скриншот настроек фильтрации почты</summary>

![Главная страница](./screenshots/фильтрация_почты.png)
</details>


<details>
<summary>Скриншот папки писем Новости</summary>

![Главная страница](./screenshots/папка_новости.png)
</details>


<details>
<summary>Скриншот папки писем proglib</summary>

![Главная страница](./screenshots/папка_proglib.png)
</details>


## 🐍 Запуск на Linux/Windows

**1) Установка wkhtmltopdf**

Библиотека `wkhtmltopdf` используется под капотом в `pdfkit`, который преобразует веб-страницу с новостями от Proglib AI в PDF

Установка wkhtmltopdf на Linux
```
sudo apt-get install wkhtmltopdf
```

Установка wkhtmltopdf для других систем
https://github.com/JazzCore/python-pdfkit/wiki/Installing-wkhtmltopdf

**2) Создание и активация виртуального окружения (опционально)**

*Linux*
```
python3 -m venv env
source env/bin/activate
```

*Windows*
```
python -m venv env
env\Scripts\activate
```

**3) Установка зависимостей**  

```
pip install -r requirements.txt
```

**6) Запуск бота**  

```
python3 main.py
```


## 📱 Запуск на Android

### Установка Termux и python на Android

**1) Загрузить Termux с GitHub или Fdroid, установить**  

Ссылки откуда можно скачать Termux  
https://github.com/termux/termux-app/releases  
https://f-droid.org/ru/packages/com.termux/  

**2) При первом запуске выполнить команду чтобы termux видел память телефона**

```
termux-setup-storage
```
Дать необходимые разрешения если спросит

**3) Обновить репозиторий и установить python**

```
pkg update
pkg install -y python
pip install -U pip
```

Проверить что python установлен
```
python -V
```
На момент декабря 2024г устанавлиывается версия 3.12.7

Опционально установить другие частоиспользуемые библиотеки
```
pkg install wget curl nano git openssh
```

### Установка wkhtmltopdf на Android для работы с PDF

Ввести команды в Termux
```
pkg update
pkg install x11-repo
pkg install wkhtmltopdf
```

Проверка версии wkhtmltopdf
```
wkhtmltopdf --version
```

### Запуск бота на Android

Чтобы не вводить команды на клавиатуре телефона, можно подключится к нему с ПК по SSH  
Пример как это сделать через терминал или яерез программу MobaXterm описано в Google Colab <a href="https://colab.research.google.com/github/sergey21000/telegram-stt-tts-bot/blob/main/Chatbot_Telegram_TTS_STT_llamacpp_Docker.ipynb"><img src="https://img.shields.io/static/v1?message=Open%20in%20Colab&logo=googlecolab&labelColor=5c5c5c&color=0f80c1&label=%20" alt="Open in Colab"></a> ноутбуке

**1) Клонирование репозитория и установка зависимостей**

Переход в папку памяти телефона, создание папки `/info_bot`, клонирование репозитория и установка зависимостей
```
cd /sdcard
mkdir -P info_bot
git clone https://github.com/sergey21000/telegram-email-news-bot.git
pip install -r requirements.txt
```

**3) Запуск бота**

```
python main.py
```

**4) Настроить Android чтобы он не закрывал Termux (опционально, если бот будет деплоиться на телефоне)**

Для этого в зависимости от версии Android нужно найти все настройки, которые отвечают за закрытие приложения системой Android (оптимизация батареи, настройка работы программ в фоновом режиме)  
Например на Android 11 (оболочки тоже могут быть разные) нужно удерживать тап на Termux - О приложении, включить автозапуск и установить Констроль активности - Без ограничений  

<details>
<summary>Скриншот Настроек Контроля активности</summary>

![Главная страница](./screenshots/main_page.png)
</details>

Так же в строке состояния Android там где висит Termux нужно нажать `Acquire wakelock`, переключив таким образом Termux в нужный режим  
Еще можно нажать на недавние приложения - тап на окошко Termux и нажать на замок чтобы закрепить приложение  

<details>
<summary>Скриншот Acquire wakelock Termux</summary>

![Главная страница](./screenshots/main_page.png)
</details>


## 🔧 Настройка рассылки

**1) Получение своего ID в Telegram (необязательный шаг)**
 
Чтобы узнать свой ID, нужно запустить бота, полученного на шаге [Подготовка к запуску](#-Подготовка-к-запуску) (`Регистрация бота`) и отправить ему команду /getid  
Бот ответит сообщеним с полем `ID чата`, скопировать его и вставить в файл `configs/chats_config.py` в переменную `ADMIN_CHAT_ID`, например
```python
ADMIN_CHAT_ID = 123456789
```
Так же узнать свой ID можно, написав боту https://t.me/getmyid_bot команду /start

Далее два варианта настройки рассылки - чтобы бот присылал рассылку в лс или чтобы он присылал ее в группу

**2) Настройка чатов для рассылки**

1. Настройка для рассылки в лс

Для того чтобы бот присылал рассылку в лс, записать свой `ADMIN_CHAT_ID`, полученный на предыдущем шаге в список `CHATS_IDS_TO_SEND` в файле `configs/chats_config.py`, например
```python
CHATS_IDS_TO_SEND = [
    123456789,
]
```

2. Настройка для рассылки в группу/группы

Если нужно чтобы бот отправлял рассылку в определенную группу, нужно его добавить в эту группу и сделать админом  
Затем запустить бота, и отправить команду /getid в группу  
Бот пришлет в лс сообщение с `ID чата`, который нужно добавить в список `CHATS_IDS_TO_SEND` в файле `configs/chats_config.py`, например
```python
CHATS_IDS_TO_SEND = [
	-1234567890123,
]
```

Список чатов можно пополнять другими ID, главное что если это группа то бот должен быть в ней админом, а если это ID чата личной переписки, то человек должен первый написать боту

## 🛠 Дополнительные настройки рассылки

**3) Настройка/добавление/удаление конфигов рассылки**

Конфиги рассылки находятся в классе `Config` в файле `configs/send_config.py`

Конфиг `proglib_email_config` отвечает за отправку рассылки от Proglib AI
Конфиг `habr_email_config` отвечает за отправку рассылки от Хабр

Письма от Хабр приходят в папку почты Новости, она уже установлена в конфиге рассылки `habr_email_config` в файле `configs/send_config` в строке `mail_folder='INBOX/News'  
Письма от Proglib AI приходят в папку почты proglib, ее нужно создать и настроить фильтр чтобы все сообщения от Proglib AI приходили туда

Для отключения конфига рассылки нужно закомментировать его
Настроить расписание рассылки можно в экземпляре класса `ScheduleKwargsConfig`, который передается в каждый конфиг рассылки

При текущих настройках рассылка новостей от Хабр приходит каждый день в 12:30  
Рассылка от Proglib AI приходит каждую субботу в 12:30

Если новая рассылка еще не пришла на почту, то парсер делает 5 повторных попыток каждые полчаса  
Количество и интервал попыток настраиваются в классе `SendSettingsConfig` в файле `configs/send_config.py`  
При достижении 5 неудачных попыток админу в лс придет сообщение об ошибке  
Отключить это можно в том же классе `SendSettingsConfig` в параметре `send_errors_to_admin`

Для активации конфига с напоминанием расскомментировать конфиг `test_reminder_config` и установить свое раписание  
Данный конфиг предназначался чтобы отправлять в чат студентов ссылку для входа на платформу для вебинаров и время встречи как напоминание о том что сегодня встреча  

**Пример создания своего конфига**

Пример создания своего простого конфига со своим текстом рассылки, который отправляет сообщение каждый понедельник и четверг в 12:00  

1) Создать конфиг в файле `config/config_classes.py` и отнаследовать его от класса `ScheduleBaseConfig`, например

```python
@dataclass
class MyReminderConfig(ScheduleBaseConfig):
    message_to_send: str
```

2) Добавить импорт в файле `config/send_config.py`

```python
from .config_classes import MyReminderConfig
```


3) Инициализировать новый конфиг в классе `Config` в файле `configs/send_config.py`  
Название конфига должно заканчиваться на `_reminder_config`

```python
@dataclass
class Config:
	# ....
    my_reminder_config: ScheduleReminderConfig = field(default_factory=lambda: ScheduleReminderConfig(
        schedule_config=ScheduleKwargsConfig(
            day_of_week='*',  # дни рассылки (mon,tue,wed,thu,fri,sat,sun)
            hour=12,  # часы рассылки
            minute=00,  # минуты рассылки
            end_date='2025-02-01',  # дата окончания рассылки
            timezone=TIMEZONE,  # часовой пояс
        ),
        admin_chat=ADMIN_CHAT,  # чат админа для отправки отчетов об ошибках
        chats=CHATS_TO_SEND,  # чаты для рассылки
        
        # функция получения сообщения для рассылки
        # принимает текущий экземпляр конфига ScheduleReminderConfig
        parse_func=EmailParser.get_reminder_send,
		
        # доп аргументы, присущие конкретному конфигу
        message_to_send = 'Здесь сообщение-напоминание для отправки по расписанию'
        )
```

Функция, которая передается в параметр `parse_func=` может быть любой, при этом она должна быть асинхронной и принимать на вход экземпляр текущего конфига, и возвращать итоговый текст для отправки  
В данном примере функция `parse_func=EmailParser.get_reminder_send` просто извлекает из текущего конфига атрибут `message_to_send` и возвращает его

Все конфиги по умолчанию производят отправку в чаты, отпределенные в списке `CHATS_IDS_TO_SEND` в файле `configs/chats_config.py`, но можно вручную указать другие чаты для каждого конкретного конфига в параметре `chats`

Для добавления своего конфига для парсинга папки писем нужно в главный конфиг `Config` добавить `ScheduleEmailConfig`, в котором описать:
 - `schedule_config` - расписание отправки рассылки (дни недели, часы, минуты)
 - `admin_chat` - чат админа куда нужно отправлять инфу об ошибках отправки рассылки
 - `chats` - список чатов куда нужно отправлять рассылку
 - `parse_func` - любая асинхронная функция которая возвращает итоговый текст для рассылки
 - `target_email_sender` - имя отправителя письма, которое нужно парсить
 - `mail_folder` - папка писем которую нужно парсить


## Лицензия

Этот проект лицензирован на условиях лицензии [MIT](./LICENSE).
